<h2>Edit unit attribute</h2>

<% form_for(@unit_attribute) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'fields', :locals => { :f => f } %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>